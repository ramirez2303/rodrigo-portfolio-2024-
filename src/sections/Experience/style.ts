import styled from "styled-components";

export const ExperienceWrapper = styled.section`
    width: 100vw;
    height: 100vh;

    background: linear-gradient(
        360deg,
        rgba(141, 65, 65, 0.2) 0%,
        rgba(107, 164, 238, 0.2) 100%
    );
    display: flex;
    justify-content: center;
    align-items: center;

    position: relative;
`;
